# Offline Character Data

- Files follow `u{codepoint}.json`, e.g. `u6c38.json` for “永”.
- JSON structure matches `hanzi-writer-data` (`strokes`, `medians`, optional `radStrokes`).
- Add more characters by copying from https://cdn.jsdelivr.net/npm/hanzi-writer-data@2.0.1/.
